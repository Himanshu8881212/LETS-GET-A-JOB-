% ---------- ATS-Friendly Layout ----------
\setlength{\parskip}{0pt}
\setlength{\parindent}{0pt}

% Balanced spacing for readability while maintaining 1-page constraint
\setlist[itemize]{leftmargin=0.15in, topsep=1pt, itemsep=1.5pt, parsep=0pt}

\renewcommand{\familydefault}{\sfdefault}

% Section formatting - with separator and proper line breaks
\newcommand{\SectionHeading}[1]{%
  \vspace{10pt}
  \noindent
  {\fontsize{11}{13}\selectfont\bfseries #1}
  \par
  \vspace{-2pt}
  \noindent
  \rule{\textwidth}{0.4pt}
  \par
}

% Better spacing for readability
\raggedbottom
\setlength{\parskip}{0pt}
