% Header with contact information - only show if name exists
\ifx\FirstName\empty\else
\begin{center}
  {\fontsize{14}{16}\selectfont\bfseries \FirstName{} \LastName{}}
  \\[2pt]
  % Contact line - only show if at least one exists
  \ifx\Phone\empty
    \ifx\Email\empty\else
      \Email{}
    \fi
  \else
    \Phone{}
    \ifx\Email\empty\else{} | \Email{}\fi
  \fi
  % Links line - only show if at least one exists
  \ifx\LinkedIn\empty
    \ifx\GitHub\empty\else
      \\[1pt]
      \GitHub{}
    \fi
  \else
    \\[1pt]
    \LinkedIn{}
    \ifx\GitHub\empty\else{} | \GitHub{}\fi
  \fi
\end{center}
\vspace{10pt}
\fi
